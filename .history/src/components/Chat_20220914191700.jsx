import React, {useState, useEffect, useRef} from 'react'

const style = {
  main: `flex flex-col p-[10px] relative`
}

const Chat = () => {
  const scroll = useRef
  return (
    <div>Chat</div>
  )
}

export default Chat